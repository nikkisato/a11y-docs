import AxeBuilder from '@axe-core/playwright'
import { expect, test } from '@chromatic-com/playwright'

// test.describe('footer', () => {
//   test('there is an h2 for Footer', async ({ page }) => {
//     await page.goto('/')

//     const response = await page.waitForSelector('h2')

//     expect(response).not.toBeNull()
//   })

//   test('there is an contentinfo landmark role for footer', async ({ page }) => {
//     await page.goto('/')

//     const response = await page.waitForSelector('[role="contentinfo"]')

//     expect(response).not.toBeNull()
//   })
// })
